apiVersion: v1
kind: ServiceAccount
metadata:
  name: fishmesh-analyst
  annotations:
    fishmesh.io/security-note: "MVP analyst is read-only and does not call the Kubernetes API"
automountServiceAccountToken: false
