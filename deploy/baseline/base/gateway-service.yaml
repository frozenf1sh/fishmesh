apiVersion: v1
kind: Service
metadata:
  name: fishmesh-gateway
  labels:
    app.kubernetes.io/name: fishmesh-gateway
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: fishmesh-gateway
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
