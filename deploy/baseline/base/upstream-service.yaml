apiVersion: v1
kind: Service
metadata:
  name: qwen-vllm-baseline
  labels:
    app.kubernetes.io/name: qwen-vllm-baseline
  annotations:
    fishmesh.io/purpose: random-clusterip-baseline
spec:
  type: ClusterIP
  sessionAffinity: None
  selector:
    app.kubernetes.io/name: qwen-vllm
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
